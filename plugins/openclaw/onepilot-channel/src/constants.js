// User-agent string the plugin attaches to outbound requests. Kept
// deliberately neutral so server logs show one identifier regardless of
// which host the plugin runs on.
export const PLUGIN_UA = "onepilot-channel/0.3.1";
